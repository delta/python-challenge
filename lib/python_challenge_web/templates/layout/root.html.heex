<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
	  <meta property="og:title" content="Python Challenge">
	  <meta property="og:description" content="A Python programming challenge organized by Delta Force">
    <%= csrf_meta_tag() %>
    <title>Python Challenge</title>
    <link phx-track-static rel="stylesheet" href={System.get_env("BASE_URI") <> Routes.static_path(@conn, "/assets/app.css")}/>
    <link phx-track-static rel="icon" type="image/png" href={System.get_env("BASE_URI") <> Routes.static_path(@conn, "/favicon.ico")}/>
    <script defer phx-track-static type="text/javascript" src={System.get_env("BASE_URI") <> Routes.static_path(@conn, "/assets/app.js")}></script>
  </head>
  <body>
      <nav>
        <div class="menu">
            <p class="website_name">
              <a href="/">
                <img src="/images/logo.png" height="40px" width="40px">
              </a>
            </p>
            <div class="menu_links">
            <a href={System.get_env("BASE_URI") <> Routes.leaderboard_path(@conn, :first_year)} class="link">Leaderboard</a>
            <%= if Plug.Conn.get_session(@conn, :email) == nil do %>
                <a href={System.get_env("BASE_URI") <> Routes.auth_path(@conn, :login)} class="link">Login</a>
            <% else %>
                <a href={System.get_env("BASE_URI") <> Routes.challenge_path(@conn, :index)} class="link">Challenges</a>
                <a href={System.get_env("BASE_URI") <> Routes.auth_path(@conn, :logout)} class="link">Logout</a>
            <% end %>
            </div>
            <div class="menu_icon">
                <span class="icon"></span>
            </div>
        </div>
    </nav>
    <%= @inner_content %>
  </body>
</html>
