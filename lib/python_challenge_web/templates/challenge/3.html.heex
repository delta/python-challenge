<script type="text/javascript">
async function initialize(pyodide) {
    await pyodide.runPythonAsync(`
        a = 1
        b = 1
    `);
    $('#content').html(`
<h1>
    Challenge 3
</h1>
<p>
    There are two variables "a" and "b" in the console. First, try running "a is b" in the console. To pass this challenge, all you have to do is find the
 least positive integer "x" such that the value of a and b are both x, but "a is b" is false.
</p>
    `)
}
</script>
